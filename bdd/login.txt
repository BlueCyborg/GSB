CREATE TABLE LOGIN(
    LOG_ID  INT PRIMARY KEY NOT NULL,
    LOG_LOGIN   VARCHAR(50) NOT NULL UNIQUE,
    LOG_MOTDEPASSE  VARCHAR(255)    NOT NULL
    ,CONSTRAINT PK_LOG_ID PRIMARY KEY (LOG_ID)
);
ALTER TABLE VISITEUR
ADD CONSTRAINT FK_LOGIN
FOREIGN KEY (LOG_LOGIN) REFERENCES LOGIN(LOG_LOGIN)